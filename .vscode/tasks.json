{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build pixels",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "-fopenmp",
                "${workspaceFolder}/main.cpp",
                "${workspaceFolder}/loadpng.cpp"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
